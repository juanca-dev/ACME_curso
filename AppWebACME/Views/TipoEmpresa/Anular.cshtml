@* @model Models.ACME.TipoEmpresaEntidad

@{
    ViewData["Title"] = "Anular";
}

<h3>¿Está seguro de anular el registro?</h3>

<div class="card">
    <div class="card-header">
        <h5>TipoEmpresa - Anular</h5>
    </div>
    <div class="card-body">
        <form asp-action="Anular" method="post" class="row mb-3">
            <!-- Campos del formulario -->
            
<div class="row mb-3">
<div class="col-md-4">
        <label asp-for="IDTipoEmpresa" class="col-sm-4 col-form-label">@Html.DisplayNameFor(model => Model.IDTipoEmpresa)</label>
        <input type="number" min="0" class="form-control" asp-for="IDTipoEmpresa"  readonly #VALORPREDETERMINADO# />
        <span asp-validation-for="IDTipoEmpresa" class="text-danger"></span>
    </div>
		<div class="col-md-4">
        <label asp-for="TipoEmpresa" class="col-sm-4 col-form-label">@Html.DisplayNameFor(model => Model.TipoEmpresa)</label>
        <input type="text" class="form-control" asp-for="TipoEmpresa" disabled  #VALORPREDETERMINADO# />
        <span asp-validation-for="TipoEmpresa" class="text-danger"></span>
    </div>
		<div class="col-md-4">
        <label asp-for="Descripcion" class="col-sm-4 col-form-label">@Html.DisplayNameFor(model => Model.Descripcion)</label>
        <input type="text" class="form-control" asp-for="Descripcion" disabled  #VALORPREDETERMINADO# />
        <span asp-validation-for="Descripcion" class="text-danger"></span>
    </div>
</div>

<div class="row mb-3">

		<div class="col-md-4">
        <label asp-for="Sigla" class="col-sm-4 col-form-label">@Html.DisplayNameFor(model => Model.Sigla)</label>
        <input type="text" class="form-control" asp-for="Sigla" disabled  #VALORPREDETERMINADO# />
        <span asp-validation-for="Sigla" class="text-danger"></span>
    </div>
		<div class="col-md-4">
        <label class="col-sm-4 col-form-label" asp-for="Activo">@Html.DisplayNameFor(model => Model.Activo)</label>
        <div class="form-check">
            <input class="form-check-input" type="checkbox" asp-for="Activo" disabled  >
        </div>
    </div>
</div>


            <div class="col-12">
                <button type="submit" class="btn btn-primary">Anular</button>
                <a class="btn btn-secondary" asp-action="IndexPaginado">Cancelar</a>
            </div>
        </form>
    </div>
</div>


 *@
@model Models.ACME.TipoEmpresaEntidad

@{
    ViewData["Title"] = "Anular tipo de empresa";
}

@* ── Encabezado de módulo ── *@
<div class="mod-header">
    <div>
        <h1 class="mod-title">Tipo de empresa</h1>
        <p class="mod-sub">Anular registro · Código @Model.IDTipoEmpresa</p>
    </div>
    <a asp-action="IndexPaginado" class="form-back" style="margin-bottom:0">
        ← Volver al listado
    </a>
</div>

@* ── Aviso de confirmación ── *@
<div class="toast toast-err" style="max-width:640px;margin-bottom:1.25rem">
    <i class="bi bi-exclamation-triangle-fill" style="font-size:1.1rem;flex-shrink:0"></i>
    <div>
        <strong>¿Está seguro de anular este registro?</strong><br>
        <span style="font-size:.82rem;opacity:.85">
            Esta acción desactivará el tipo de empresa <strong>@Model.TipoEmpresa</strong>
            (<span style="font-family:monospace">@Model.Sigla</span>).
            Podrá reactivarlo desde Modificar.
        </span>
    </div>
</div>

@* ── Card de datos (solo lectura) ── *@
<div class="form-card" style="max-width:640px">
    <form asp-action="Anular" method="post">

        <input type="hidden" asp-for="IDTipoEmpresa" />

        <div class="form-section">Identificación</div>

        <div class="form-row">
            <div class="form-group">
                <label class="form-label">Código</label>
                <div class="form-input" style="background:#f9fafb;color:#9ca3af;cursor:default">
                    @Model.IDTipoEmpresa
                </div>
            </div>
            <div class="form-group">
                <label class="form-label">Sigla</label>
                <div class="form-input" style="background:#f9fafb;color:#374151;cursor:default;font-family:monospace;font-weight:600">
                    @Model.Sigla
                </div>
            </div>
        </div>

        <div class="form-group">
            <label class="form-label">Nombre del tipo</label>
            <div class="form-input" style="background:#f9fafb;color:#374151;cursor:default;font-weight:600">
                @Model.TipoEmpresa
            </div>
        </div>

        <div class="form-group">
            <label class="form-label">Descripción</label>
            <div class="form-input" style="background:#f9fafb;color:#374151;cursor:default;min-height:70px">
                @if (!string.IsNullOrEmpty(Model.Descripcion))
                {
                    @Model.Descripcion
                }
                else
                {
                    <span style="color:#d1d5db;font-style:italic">Sin descripción</span>
                }
            </div>
        </div>

        <div class="form-group">
            <label class="form-label">Estado actual</label>
            <div style="padding-top:.4rem">
                @if (Model.Activo == true)
                {
                    <span class="badge badge-green">
                        <svg width="6" height="6" viewBox="0 0 6 6" style="flex-shrink:0"><circle cx="3" cy="3" r="3" fill="#059669" /></svg>
                        Activo
                    </span>
                }
                else
                {
                    <span class="badge badge-red">
                        <svg width="6" height="6" viewBox="0 0 6 6" style="flex-shrink:0"><circle cx="3" cy="3" r="3" fill="#dc2626" /></svg>
                        Inactivo
                    </span>
                }
            </div>
        </div>

        <div class="form-footer">
            <a class="btn-cancel" asp-action="IndexPaginado">Cancelar</a>
            <button type="submit" class="btn-save" style="background:#dc2626"
                    onmouseover="this.style.background='#b91c1c'"
                    onmouseout="this.style.background='#dc2626'">
                <i class="bi bi-trash"></i> Confirmar anulación
            </button>
        </div>

    </form>
</div>
